{% extends '@NetBSCore/searcher/searcher.html.twig' %}

{% block searchForm %}

    {{ form_start(form) }}
    <div class="row">
        <div class="col-md-3">
            <h4>Description</h4>
            {{ form_row(form.titre) }}
        </div>
        <div class="col-md-3">
            <h4>Montant</h4>
            {{ form_row(form.montant) }}
        </div>
        <div class="col-md-3">
            <h4>Date de création</h4>
            {{ form_row(form.date) }}
        </div>
        <div class="col-md-3">
            <h4>Rabais</h4>
            {{ form_row(form.rabais) }}
        </div>

        <button class="btn btn-primary">Rechercher</button>
    </div>
    {{ form_end(form) }}

{% endblock %}